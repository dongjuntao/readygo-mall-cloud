<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mall.admin.mapper.AdminUserMapper">

    <!--根据用户主键id查询用户信息-->
    <select id="getAdminUserById" resultType="com.mall.admin.entity.AdminUserEntity">
        SELECT * FROM admin_user au where id = #{id};
    </select>
    <!--根据用户名查询用户信息-->
    <select id="getAdminUserByUserName" resultType="com.mall.admin.entity.AdminUserEntity">
        SELECT * FROM admin_user au WHERE user_name = #{userName};
    </select>
    <!--根据用户id查询所有的菜单id-->
    <select id="queryAllMenuId" resultType="Long">
        SELECT srm.menu_id FROM sys_role_menu srm
            LEFT JOIN sys_user_role sur ON srm.role_id=sur.role_id
        WHERE sur.user_id = #{userId};
    </select>
    <select id="queryAllPerms" resultType="String">
        SELECT distinct m.perms FROM user_role ur
            LEFT JOIN role_menu rm ON ur.role_id=rm.role_id
            LEFT JOIN menu m ON m.id=rm.menu_id
        WHERE ur.user_id = #{userId};
    </select>

</mapper>